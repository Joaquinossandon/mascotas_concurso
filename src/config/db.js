const config = {
    host: "localhost",
    port: 5433, // 5432
    database: "mascotas_concursos",
    username: "postgres",
    password: "admin123",
    dialect: "postgres",
    logging: false,
};

module.exports = config;
